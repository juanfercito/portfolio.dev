---
const { class: className, id } = Astro.props;
---

<section
  id={id}
  data-section={id}
  class={`section w-full mx-auto lg:w-[740px] ${className} w-full mx-auto container lg:max-w-4xl md:max-w-2xl`}
>
  <slot />
</section>
